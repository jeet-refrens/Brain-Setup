---
description: Thin backwards-compatible alias for /tactics-board
argument-hint: "[--model fable|opus|sonnet|<model-id>] [--effort low|medium|high|xhigh] [--scout auto|sonnet|haiku|none] [--scout-effort auto|low|medium|high|xhigh] <task or decision to plan>"
---

This is a backwards-compatible alias for `/tactics-board`.

Follow `/tactics-board` exactly. This alias exists only for muscle memory.

Use the canonical tactics-board defaults unless flags override them: scout `auto`, planner `fable`, planner effort `high`.

Do not implement, edit files, delegate implementation, or start orchestration.

Task: $ARGUMENTS
