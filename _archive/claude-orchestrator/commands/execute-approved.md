---
description: Thin backwards-compatible alias for /kickoff-approved
argument-hint: <approved plan or approved task scope>
---

This is a backwards-compatible alias for `/kickoff-approved`.

Follow `/kickoff-approved` exactly. This alias exists only for muscle memory.

This command is only for work the human has already approved. Use the canonical kickoff-approved team defaults: Opus orchestrator, optional Luna scout only when context is missing, Terra implementer, Sol reviewer, `extra-time` loop, `clean-sheet`, then `captains-call`.

Approved work: $ARGUMENTS
